// Unified blueprint system: prázdný manifest - audio se načítá přes AudioLoader
// Funkce ponechána pro backward compatibility

export function buildSfxManifest() {
  // Prázdný manifest - nový systém používá AudioLoader
  return new Map();
}