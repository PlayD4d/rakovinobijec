{
  id: 'powerup.oxidative_burst',
  type: 'powerup',
  stats: {
    maxLevel: 5,
  },
  mechanics: {
    modifiersPerLevel: [],
    stackable: true,
    persistent: true,
    applyOnPickup: true,
    effectType: 'flamethrower',
    slot: 'weapon',
  },
  ability: {
    type: 'flamethrower',
    // Slow-travelling flame cone toward nearest enemy
    // Damages enemies as the cone passes through them over 2-3s
    damagePerLevel: [8, 12, 18, 25, 35],
    intervalMsPerLevel: [5000, 4000, 3000, 2000, 1000],
    // range = how far the flame cone travels (px)
    speedPerLevel: [160, 180, 200, 220, 250],
    // radius = cone width at the end (px) — scales with area_boost
    radiusPerLevel: [35, 40, 45, 50, 55],
    // duration = how long the flame takes to travel (ms)
    durationMsPerLevel: [2500, 2500, 2000, 2000, 1500],
    // damage ticks per second while cone is active
    ticksPerSec: 4,
  },
  vfx: {
    pickup: 'vfx.pickup',
    levelUp: 'vfx.powerup',
    impact: 'vfx.hit.medium',
  },
  sfx: {
    pickup: 'sound/pickup.mp3',
    apply: 'sound/powerup.mp3',
    levelUp: 'sound/powerup.mp3',
  },
  display: {
    key: 'powerup.oxidative_burst.name',
    descKey: 'powerup.oxidative_burst.desc',
    templates: {
      short: '{{ability.damagePerLevel}} DMG • {{ability.intervalMsPerLevel}}ms',
      desc: 'Periodicky vysílá ohnivý oblak na nejbližšího nepřítele.',
    },
    color: '#FF6600',
    rarity: 'rare',
    icon: 'powerup.oxidative_burst',
    category: 'special',
    sortOrder: 310,
    tags: ['powerup', 'special', 'fire', 'auto-aim', 'pierce'],
    devNameFallback: 'Oxidative Burst',
    devDescFallback: 'Auto-aimed fire projectiles that pierce through enemies.',
  },
}
